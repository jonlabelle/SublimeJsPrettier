<snippet>
  <content><![CDATA[{
  // ---------------------------------------------------------------------------
  // The line length that the printer will wrap on
  // ---------------------------------------------------------------------------

  //"printWidth": 80,

  // ---------------------------------------------------------------------------
  // The number of spaces per indentation-level
  // ---------------------------------------------------------------------------

  //"tabWidth": 2,

  // ---------------------------------------------------------------------------
  // Indent lines with tabs instead of spaces
  // ---------------------------------------------------------------------------

  //"useTabs": false,

  // ---------------------------------------------------------------------------
  // Print semicolons at the ends of statements
  // ---------------------------------------------------------------------------

  //"semi": true,

  // ---------------------------------------------------------------------------
  // Use single quotes instead of double quotes
  // ---------------------------------------------------------------------------

  //"singleQuote": false,

  // ---------------------------------------------------------------------------
  // Change when properties in objects are quoted (as-needed|consistent|preserve)
  // ---------------------------------------------------------------------------

  //"quoteProps": "as-needed",

  // ---------------------------------------------------------------------------
  // Use single quotes instead of double quotes in JSX
  // ---------------------------------------------------------------------------

  //"jsxSingleQuote": false,

  // ---------------------------------------------------------------------------
  // Print trailing commas wherever possible in multi-line comma-separated
  // syntactic structures (es5|none|all)
  // ---------------------------------------------------------------------------

  //"trailingComma": "es5",

  // ---------------------------------------------------------------------------
  // Print spaces between brackets in object literals
  // ---------------------------------------------------------------------------

  //"bracketSpacing": true,

  // ---------------------------------------------------------------------------
  // Put the > of a multi-line HTML (HTML, JSX, Vue, Angular) element
  // at the end of the last line instead of being alone on the next line
  // (does not apply to self-closing elements)
  // ---------------------------------------------------------------------------

  //"bracketSameLine": false,

  // ---------------------------------------------------------------------------
  // Include parentheses around a sole arrow function parameter (always|avoid)
  // ---------------------------------------------------------------------------

  //"arrowParens": "always",

  // ---------------------------------------------------------------------------
  // Only format files that contain a pragma comment at the top of the file
  // ---------------------------------------------------------------------------

  //"requirePragma": false,

  // ---------------------------------------------------------------------------
  // Insert a special @format marker at the top of files specifying that the
  // file has been formatted with Prettier
  // ---------------------------------------------------------------------------

  //"insertPragma": false,

  // ---------------------------------------------------------------------------
  // In Markdown files, wrap prose to the print width (always),
  // force all prose blocks to be on a single line (never),
  // or do nothing and leave prose as-is (preserve)
  // ---------------------------------------------------------------------------

  //"proseWrap": "preserve",

  // ---------------------------------------------------------------------------
  // Set the global whitespace sensitivity for HTML, Vue, Angular, and Handlebars:
  // - css     respect the default value of CSS display property
  // - strict  whitespace (or the lack of it) around all tags is considered significant
  // - ignore  whitespace (or the lack of it) around all tags is considered insignificant
  // ---------------------------------------------------------------------------

  //"htmlWhitespaceSensitivity": "css",

  // ---------------------------------------------------------------------------
  // Whether or not to indent the code inside <script> and <style> tags in Vue files
  // ---------------------------------------------------------------------------

  //"vueIndentScriptAndStyle": false,

  // ---------------------------------------------------------------------------
  // The character(s) used to terminate each line:
  // - lf    Line Feed only (\n)
  // - crlf  Carriage Return + Line Feed characters (\r\n)
  // - cr    Carriage Return character only (\r), used very rarely
  // - auto  Maintain existing line endings
  // ---------------------------------------------------------------------------

  //"endOfLine": "lf",

  // ---------------------------------------------------------------------------
  // Enforce single attribute per line in HTML, Vue and JSX
  // ---------------------------------------------------------------------------

  //"singleAttributePerLine": false,

  // ---------------------------------------------------------------------------
  // Overrides let you have different configuration for certain file extensions,
  // folders and specific files
  // ---------------------------------------------------------------------------

  //"overrides": [
  //  {
  //    "files": "*.example.js",
  //    "options": {
  //      "semi": true
  //    }
  //  },
  //  {
  //    "files": ["*.html", "legacy/**/*.js"],
  //    "options": {
  //      "tabWidth": 4
  //    }
  //  }
  //]
}

]]></content>
  <tabTrigger>prettierrc,prettierconfig</tabTrigger>
  <scope>source.json,text.plain</scope>
  <description>Prettier config file (JsPrettier)</description>
</snippet>
