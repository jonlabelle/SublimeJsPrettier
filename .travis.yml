language: python

# Recommended by Travis support:
sudo: required

python:
  - "2.7"
  - "3.3"

# Install dependencies:
install:
  - pip install flake8

# Run tests:
script:
  - flake8 . --max-line-length=81

notifications:
  email: false
